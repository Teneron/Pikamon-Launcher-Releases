import electron from 'electron';
import { app } from 'electron';

console.log('Electron default import:', electron);
console.log('Electron app import:', app);
console.log('process.versions:', process.versions);
